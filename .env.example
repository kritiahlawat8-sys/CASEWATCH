# .env.example
# Create a copy of this file and rename it to .env
# Do not commit .env to source control

# Local development backend URL
VITE_API_URL=http://localhost:8000

# When deploying to Vercel, set VITE_API_URL in your Vercel project settings
# to your live Render backend URL (e.g. https://your-backend.onrender.com)
